jaina@Mac 02-durable-run-relay % make stress
PYTHONPATH=. python3 stress.py
workers                  : 12
approved assets per run  : 4
objects the runs created : 48
objects the provider holds: 48
run status counts        : {'done': 11, 'running': 1}
errors raised            : 1
  JSONDecodeError: Expecting value: line 1 column 1 (char 0)

recovery pass over anything left running:
  attempt 1: returned
  attempt 2: returned
  attempt 3: returned
run status counts after  : {'done': 12}
jaina@Mac 02-durable-run-relay % make stress
PYTHONPATH=. python3 stress.py
workers                  : 12
approved assets per run  : 4
objects the runs created : 48
objects the provider holds: 47
run status counts        : {'done': 11, 'running': 1}
errors raised            : 1
  KeyError: '06890017-28e2-4a80-a8c3-4d35e9dd26bf:asset-email-003'

recovery pass over anything left running:
  attempt 1: returned
  attempt 2: returned
  attempt 3: returned
run status counts after  : {'done': 12}




Number of errors is equal to the number of workers that are running. 

Agent findings review:

Looking at agent1 and agent2 findings for concurrency they both are producing similar results. Agent 1 had 12 exact, 167
short and 21 unreadable results and agent 2 had 2 exact and 26 short and 12 unreadable results. The percentages are different
different but it seems like there is no safe concurrency limit to do things correctly

C8 seems like 3 different bugs and that is something we will need to look at more.
  1. No exclusive worker claim
     Multiple workers can process one run and duplicate provider calls.
  2. Unsafe shared provider access—the cause of the missing object
     Whole-file read-modify-write operations can overwrite newer state, lose objects, or corrupt the JSON.
  3. False and stale verification—the cause of the operator lie
     verified is unconditional, audit is not live, and completed runs are never reconciled by recovery.


Retrying a run is different than reexecuting the same run. We get 4 workers more everytime we retry but reexecuting the ssame run 
just gives us four still





  1. Now: Record case hypotheses and identify which cases authorize each repair.
  2. Implement: Write failing contract tests, make repairs, and mutation-test each cause.
  3. At the end: Produce the final c1–c11 table showing:
      - observed behavior;
      - confirmed cause or healthy behavior;
      - responsible repair/check;
      - cause versus symptom detection;
      - remaining uncertainty.
